# Django REST Framework on Docker

## Usage

1. Make .env
2. CMD

### For development
```bash
cd django-rest-api
docker-compose up
```

### For production
```bash
cd django-rest-api
docker-compose -f docker-compose.prod.yml up
```
